

https://github.com/user-attachments/assets/030c81f3-3100-45c5-95d8-0b207d06df0a


---

# 基于 OpenCV 的机甲大师装甲板灯条识别程序 —— 代码结构说明

本项目实现了一个基于 C++ 和 OpenCV 库的计算机视觉程序，用于实时识别和追踪机甲大师（RoboMaster）装甲板上的蓝白发光灯条。程序针对“高曝光光晕包黑条”的特殊场景，通过颜色空间转换与拓扑轮廓分析，精准提取出灯条核心位置并进行可视化框选与视频保存。

## 一、 系统依赖与环境

* **核心语言**：C++ 11 或更高版本
* **第三方库**：OpenCV 4.x
* **核心模块**：`opencv2/opencv.hpp`（涵盖 `videoio`, `imgproc`, `highgui` 等子模块）

---

## 二、 代码核心结构分解

程序整体采用**单线程顺序执行结构**，主要由以下四个逻辑板块组成：

### 1. 媒体流初始化与配置 (Initialization)

程序启动后，首先初始化输入输出流。

* **输入流 (`cv::VideoCapture`)**：打开指定的摄像头（或视频文件），并动态获取原始视频的物理属性，包括帧宽度（Width）、帧高度（Height）以及帧率（FPS）。
* **输出流 (`cv::VideoWriter`)**：根据输入流的编码参数、分辨率和帧率，初始化输出写入器。指定使用 `mp4v` 编码格式，确保渲染后的画面能以相同的规格实时写入 `output.mp4` 文件中。

### 2. 图像预处理与色彩分割 (Preprocessing & Color Segmentation)

主循环内，程序对获取的每一帧图像进行逐帧处理，以隔绝室内杂散光的干扰：

* **色彩空间转换 (`cv::cvtColor`)**：将图像从对光照敏感的 BGR 空间转换至 **HSV（色调、饱和度、亮度）空间**。
* **阈值化二值化 (`cv::inRange`)**：针对蓝白发光灯条产生的大范围高亮光晕，设定严格的 HSV 范围上下限。过滤后生成黑白二值化掩膜（Mask），此时发光区域在掩膜上呈现为“大白团”。
* **形态学闭运算 (`cv::morphologyEx`)**：使用 $3 \times 3$ 的矩形结构元素对掩膜进行**闭运算（先膨胀后腐蚀）**。该操作能在连通外围高亮区域的同时，保护好光晕内部由于过曝产生的“小黑条（空洞）”结构。

### 3. 双层拓扑轮廓分析与特征筛选 (Contour Analysis & Filtering)

这是程序实现精准识别的核心算法板块：

* **拓扑层级提取 (`cv::findContours`)**：使用 `cv::RETR_CCOMP` 模式提取轮廓。该模式将图像中的所有轮廓组织为两层层级结构（Hierarchy）：外层为白色光晕的边界，内层为被包裹在白团内部的黑色实体边界。
* **层级过滤 (`hierarchy[i][3] >= 0`)**：遍历所有轮廓，通过检查父轮廓索引。只有当当前轮廓拥有父轮廓时，才证明其属于“被包裹在光晕内部的黑条”，从而直接剔除背景中其他无包裹的独立杂光。
* **几何几何约束筛选**：
* **面积过滤 (`cv::contourArea`)**：过滤掉由于晃动或噪声产生的微小黑点（面积 < 10 像素）。
* **外接矩形提取 (`cv::boundingRect`)**：计算候选黑条的正外接矩形。
* **宽高比过滤 (Aspect Ratio)**：计算外接矩形的高度与宽度比值，灯条在物理上为垂直长条，因此筛选出宽高比大于 1.0 且高度在合理范围（10 ~ 300 像素）内的目标。



### 4. 实时渲染、保存与退出 (Rendering & Output)

* **绘制标志 (`cv::rectangle`)**：对通过所有筛选条件的灯条矩形，使用绿色线条（`cv::Scalar(0, 255, 0)`）在原始图像帧上进行框选。
* **文件写入 (`output_video.write`)**：将画有绿色识别框的图像帧写入目标视频流，实现同步保存。
* **交互窗口 (`cv::imshow` & `cv::waitKey`)**：在名为 `"Result"` 的窗口中实时刷新渲染结果，并监听键盘事件。若检测到用户按下 `ESC` 键（ASCII 码 27），则主动跳出循环。
* **资源释放 (Release)**：循环结束后，显式调用 `.release()` 释放摄像头和视频文件句柄，并销毁所有 GUI 窗口。

---

## 三、 数据流向图 (Data Flow)

```
[摄像头/视频输入] 
       │
       ▼
  [BGR 原始帧] ───────► (保留至后期画框)
       │
       ▼
 [HSV 空间转换]
       │
       ▼
 [inRange 二值化] ────► [生成大白团掩膜 (Mask)]
                               │
                               ▼
                        [形态学闭运算] (保护内部黑条)
                               │
                               ▼
                        [RETR_CCOMP 轮廓提取]
                               │
                               ▼
                        [层级与几何特征筛选] 
                               │ (滤除背景杂光与噪点)
                               ▼
                        [提取目标黑条矩形]
                               │
                               ▼
  [BGR 原始帧] ◄─────── [绘制绿色长方形框]
       │
       ├───────────────────────┐
       ▼                       ▼
 [output_video 编码写入]   [cv::imshow 窗口显示]
       │                       │
       ▼                       ▼
  (保存为 mp4)             (屏幕实时可见)

```


